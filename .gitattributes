# HandRanks.dat is generated locally by the build (see tools/HandRankTableGenerator.java)
# rather than stored in Git LFS. LFS is disabled on this repository, so the old
# "*.dat filter=lfs" rule made every fresh clone fail on checkout.
